{% extends 'base.html.twig' %}

{% block title %}Nouveau sujet{% endblock %}

{% block body %}
    <div class="bradcam_area bradcam_bg_1">
        <div class="container">
            <div class="row">
                <div class="col-xl-12">
                    <div class="bradcam_text">
                        <h3>Nouveau sujet</h3>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="welcome_docmed_area">
        <div class="container">
            <div class="row">
                <div class="col-xl-8 offset-xl-2">
                    <div class="welcome_docmed_info">
                        <h2>Créer un sujet</h2>
                        {{ form_start(form) }}
                            {{ form_row(form.titre) }}
                            {{ form_row(form.description) }}
                            {{ form_row(form.idUser) }}
                            {{ form_row(form.imageFile) }}
                            {{ form_row(form.isPinned) }}
                            {{ form_row(form.status) }}
                            {{ form_row(form.category) }}
                            {{ form_row(form.attachmentFile) }}
                            <button type="submit" class="boxed-btn3">Publier</button>
                            <a href="{{ path('front_forum_index') }}" class="boxed-btn3-white" style="margin-left: 10px;">Retour</a>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
